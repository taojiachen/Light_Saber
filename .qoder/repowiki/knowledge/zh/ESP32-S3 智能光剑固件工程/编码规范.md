- 所有自定义功能模块均封装为 `components/` 下的独立 IDF 组件，通过 `idf_component_register` 声明依赖与包含路径。
- 音频与多媒体处理任务（如编码/解码）强制绑定到特定 CPU 核心（Core 1），以隔离实时性要求高的音频流与主逻辑。
- 跨任务共享的环形缓冲区或全局状态均通过 FreeRTOS 信号量（`SemaphoreHandle_t`）进行互斥保护，确保线程安全。