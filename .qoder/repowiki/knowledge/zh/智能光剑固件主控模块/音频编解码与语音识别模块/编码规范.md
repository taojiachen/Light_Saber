- 所有编解码器实现均遵循统一的注册模式，通过调用`decoder_ops_register`或`encoder_ops_register`将函数指针绑定到抽象结构体。
- 动态内存分配优先使用`heap_caps_malloc`并指定`MALLOC_CAP_SPIRAM`或`MALLOC_CAP_INTERNAL`，以优化ESP32有限的内部RAM资源。
- 跨任务共享的音频数据缓冲区（如`opus_output_buffer`）均配合`SemaphoreHandle_t`互斥锁进行同步，确保读写原子性。