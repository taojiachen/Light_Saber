- 使用 FreeRTOS 队列 (`xQueueCreate`/`xQueueSend`/`xQueueReceive`) 实现生产者-消费者模式，解耦事件触发与状态处理逻辑。
- 在状态转换的关键节点（如进入 RECORDING 或返回 IDLE）执行复合操作，包括调用业务 API (`app_sr_*`) 和发送网络信令 (`ws_send_text`)。
- 通过静态全局变量 (`current_state`, `event_queue`) 维护模块内部状态，并提供只读访问接口 (`state_machine_get_current_state`) 供外部调试。