- **集中式启动编排**：在 `main.c` 的 `app_main` 中按依赖顺序串行调用各子系统初始化接口（如先 `imu_init` 后 `angle_config_manager_init`）。
- **扁平化组件集成**：通过 `CMakeLists.txt` 将 `app/` 下所有功能目录注册为单一组件，各子模块以全局头文件形式暴露接口，无复杂运行时服务发现机制。
- **硬件抽象层对接**：直接调用底层驱动接口（如 `gpio_install_isr_service`、`esp_board_init`）完成板级支持包（BSP）配置。