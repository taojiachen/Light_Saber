- **ESP-IDF Components**: 深度集成 `esp_mqtt_client`, `esp_websocket_client` (WSS/SSL), `esp_http_server` (配网), `nvs_flash` (配置持久化), `esp_spiffs` (音频存储)。
- **协议支持**: MQTT (阿里云物联网平台), WebSocket (二进制OPUS/JPEG传输), TCP (自定义应用层协议), HTTP ( captive portal)。