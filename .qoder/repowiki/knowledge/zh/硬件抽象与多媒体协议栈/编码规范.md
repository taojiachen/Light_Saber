- 所有硬件驱动组件（如 IMU, hardware_driver）均采用 C 语言编写，并通过头文件暴露不透明指针或静态实例以实现封装。
- 第三方音频库（Opus, libogg, Helix）在集成时保留了原始目录结构，并通过统一的 `idf_component_register` 机制注册到构建系统中。
- 关键硬件接口（如 I2S 读写、IMU 数据读取）均返回 `esp_err_t` 或标准错误码，确保上层应用能统一处理异常状态。