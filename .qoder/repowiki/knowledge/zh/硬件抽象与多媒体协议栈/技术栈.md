- **Opus 1.5.2**：采用 SILK+CELT 混合架构，配置为定点运算 (`FIXED_POINT=1`) 以优化嵌入式性能。
- **Helix MP3**：使用固定点 (`fixpnt`) 解码引擎，针对 ARM 平台进行了汇编级优化。
- **ESP-IDF Components**：基于 Espressif 组件管理系统，依赖 `driver`, `freertos`, `mqtt`, `spiffs` 等官方库。