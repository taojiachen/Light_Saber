- 采用不透明指针（Opaque Pointer）模式管理对象生命周期，如 `esp_tts_handle_t` 和 `esp_tts_voice_t`，通过 `create/init` 和 `destroy/free` 函数对进行操作。
- API 函数命名遵循 `esp_tts_<module>_<action>` 的统一前缀规范，例如 `esp_tts_parse_chinese` 和 `esp_tts_stream_play`。
- 所有公共头文件均使用 `extern "C"` 包裹，确保 C++ 环境下的链接兼容性。