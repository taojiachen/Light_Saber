- 采用接口与实现分离设计，`include/` 目录定义公共 API（如 `esp_mn_speech_commands.h` 和 `model_path.h`），`.c` 文件提供具体实现。
- 内部划分为命令管理子系统（基于单向链表管理语音指令及其音素）与模型加载子系统（支持 SPIFFS、SD 卡及内存映射等多种存储后端的模型解析）。
- 通过静态全局变量维护单例状态（如命令根节点与模型列表），并依赖 `esp_mn_iface` 抽象层与上层识别引擎交互。